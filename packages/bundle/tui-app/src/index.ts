/**
 * @deepseek-ai/dsh-tui-app-bundle — the interactive terminal surface as a
 * profile bundle. The package's substance is `cordis.patch.yml`, declared by
 * the `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; the runtime glue plugin it mounts lives in
 * `@deepseek-ai/dsh-tui-app`.
 * @module @deepseek-ai/dsh-tui-app-bundle
 */

export {}
